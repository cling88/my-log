import React from 'react'

import { searchList } from '../config/historyData'

const Search = () => {
    return (
        <div>
            <div className="row">
            {/* 
            
                let keyList = Object.keys(tmpData);
                tmpData[cate].map((item,j) => {
            
            */}
            </div>
        </div>
    )
}

export default Search
