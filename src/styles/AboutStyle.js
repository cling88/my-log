import {css} from '@emotion/react';

const AboutStyle = css`
    padding-top: 50px;
    .aboutmeList {
        position: relative; 
        top: 100px;
        opacity: 0; 
    }
`

export default AboutStyle;